[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gedenaz"
version = "0.1.0"
description = "Backend (API) de GedeNaz App"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
